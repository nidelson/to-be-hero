{
  "name": "@to-be-hero/backend",
  "version": "0.0.0",
  "description": "ToBeHero backend",
  "license": "ISC",
  "scripts": {
    "dev": "nodemon src/index.ts",
    "build": "yarn clean && sucrase ./src -d ./build --transforms typescript,imports",
    "clean": "rm -rf build",
    "test": "echo \"OK\""
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.3",
    "nodemon": "^2.0.2",
    "sucrase": "^3.13.0",
    "typescript": "^3.8.3"
  }
}
